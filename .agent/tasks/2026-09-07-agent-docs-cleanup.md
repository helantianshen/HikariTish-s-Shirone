# 更新和清理 .agent 文档

- 更新时间：2026-09-07（Asia/Shanghai）。
- 目标：依据当前本地规则整理 `.agent/`，隔离稳定事实、任务状态及历史记录，避免旧约束继续影响开发。
- 范围：仅 `.agent/`；不修改主题源码、原规则、历史计划或技能，不执行暂存、提交、推送和部署。
- 状态：已完成，文档一致性检查通过。

## 已核验事实

- 项目根目录和工作目录均为 `E:/front_end/Shirone`；本次在本机 Windows x64、PowerShell 7.6.5 中执行，未进入 WSL/容器/SSH 环境。Node 为 `v24.20.0`，pnpm 为 `9.14.4`，与项目声明的 pnpm 版本一致。这些是本次环境快照，不是长期环境要求。
- 修改前本地 `git status --short` 为空，`.agent/` 只有已跟踪的 `HANDOFF.md`，没有嵌套规则或其他任务文件。
- 本次读取根/相关嵌套 AGENTS、当前全局总纲和三份 policy、项目规则、技能索引及开发技能、相关架构/打包文档和三个历史计划。全局源文件实际位于 `C:/Users/80945/.codex/`；索引中不固定此机器路径。
- 本地提交 `9d4cd2a`（2026-08-31）已包含博客定制和旧交接文件，“准备提交”已过时；未核验远程或 EdgeOne。

## 修改与决策

- 新增 [PROJECT.md](../PROJECT.md)，只保留稳定事实、权威规则入口和长期边界，不延续旧任务的当前目标。
- 将原交接完整归档至 [博客定制历史任务](archive/2026-08-30-blog-personalization.md)，顶部标明原状态、测试、风险和待办均需在新任务中核验。保留原文以避免丢失内容迁移、脱敏、封面和标签等历史决策。
- 将 [HANDOFF.md](../HANDOFF.md) 缩为兼容入口：三个历史计划仍引用此路径，直接删除会制造悬空引用；旧共享状态约定在入口明确停用。
- 在 PROJECT 中指出已观察到的原子组合、CSS important、日常/提交验证、性能测量措辞差异及适用依据；未扩大范围改写源规则。

## 实际验证

- 归档正文原始字节保留：13,038 bytes；SHA-256 `CB655197C977463D10506E15A15AA14CA46F232A335B3E40A3977E45BCAD90F7`，归档写入后已比对通过。
- 文档本地链接检查通过，覆盖 `.agent/` 全部 4 份 Markdown；旧计划所需的 HANDOFF 路径仍存在。
- 对照 `package.json` 核实 pnpm 版本声明、`lint`/`format` 写入行为及相关命令入口；历史/当前状态检查通过，“准备提交并推送”的旧句仅保留于明确标注的历史快照。
- `git diff --check` 通过；Git 仅提示 HANDOFF 的 LF 将按本地设置转换为 CRLF，不是空白错误。修改范围仅为 `.agent/`：1 份原文件更新、3 份新增文档；未暂存、提交或推送。
- 未执行：Astro check、Biome、构建、type-check、Playwright、包模式冒烟；本次仅修改 `.agent/` 文档。没有把旧记录中的通过或失败数字作为本次结论。

## 风险与剩余工作

- `.agent/` 是开发入口和状态管理文档，不能自动强制所有未来开发遵循规则；执行者仍须按具体任务读取原文并通过对应门禁。
- 原规则及历史计划中的措辞差异仍存在；本次在入口提示，没有替它们声称已统一。
- 旧记录中的部署域名、个人资料待办、演示页测试失败、type-check 错误和预览服务均不是本次待处理工作；继续相应任务时重新检查。
- 本次范围无剩余工作。下一次开发按 PROJECT 入口读取当前权威规则，并匹配或创建独立任务文件。
